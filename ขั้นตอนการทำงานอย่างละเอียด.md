# ขั้นตอนการทำงานและ System Architecture (เจาะลึกการแตกฝั่ง Frontend User/Admin)

เอกสารฉบับนี้อธิบายลอจิกการทำงานเชิงลึก (Deep Technical Flow) เพื่อใช้ในรายงานโปรเจกต์ระดับอุดมศึกษา โดยเจาะลึก **การทำงานที่แยกจากกันอย่างเด็ดขาดระหว่างผู้ใช้งานทั่วไป (User) และผู้ดูแลระบบ (Admin) บนระบบพอร์ทัล Frontend** พร้อมการเชื่อมโยงกับ Backend API และโครงสร้างฐานข้อมูลแบบละเอียดยิบ

---

## 1. การทำงานฝั่งผู้ใช้งานทั่วไป (User / Customer Portal)
*(โดเมนการใช้งาน: สมัครสมาชิก, เลือกซื้อ, ติดตามสถานะ, รายการโปรด และคอมเมนต์รีวิว)*

**ข้อจำกัดทางสิทธิ์การเข้าถึง (Access Control Limitations):** 
- ลูกค้า ไม่สามารถ สแกนใบหน้าได้ (ลายนิ้วมือ/ใบหน้าสงวนไว้สำหรับความมั่นคงระดับ Super Admin เท่านั้น)
- ลูกค้า ไม่สามารถ พิมพ์ไฟล์ใบเสร็จ (PDF) ชนิดเต็มรูปแบบได้ (สงวนสิทธิ์ให้แอดมินปริ้นท์สำหรับแนบลงกล่องพัสดุ ป้องกันการปลอมแปลง)
- ลูกค้า ไม่มีสิทธิ์ เข้าถึงหน้าแดชบอร์ดหลังบ้านใดๆ

---

## 0. ส่วนประกอบร่วม (Common Components)
ส่วนนี้คือฟังก์ชันที่ปรากฏอยู่ในทุกหน้าเพื่อความสะดวกในการใช้งาน

### 0.1 แถบเมนูนำทาง (Navigation Bar / Header)
เป็นส่วนควบคุมหลักที่ปรากฏทุกหน้า เพื่อเชื่อมโยงไปยังส่วนต่างๆ ของระบบ
- **การนำทาง (Navigation Logic - กดแล้วไปไหน?):** 
  - **Velin Logo:** กลับสู่หน้าหลัก (`/`)
  - **หน้าหลัก (Home):** กลับสู่หน้าหลัก (`/`)
  - **สินค้า (Products):** ไปยังหน้ารวมสินค้าทั้งหมด (`/products`)
  - **หมวดหมู่ (Categories):** เปิด Mega Menu เพื่อเลือกการกรองแบบละเอียด (ตามรูปภาพประกอบ)
    - **แบรนด์ (Brands):** เลือกตามยี่ห้อ (Nike, Adidas, etc.) -> ไปหน้า `/products?brand=...`
    - **ประเภทสินค้า (Types):** เลือกระหว่าง เสื้อผ้า หรือ รองเท้า -> ไปหน้า `/products?category=...`
    - **ราคา (Price Range):** กรองตามงบประมาณ (ตามรูปภาพประกอบ)
      - **Price Slider:** เลือกช่วงราคาด้วยการลากปุ่ม (0 - 100,000 บาท)
      - **Price Presets:** ทางลัดเลือกช่วงราคา เช่น "ต่ำกว่า 5,000" หรือ "50,000 ขึ้นไป"
    - **คอลเลกชันพิเศษ:** เลือกสินค้าใหม่, ยอดนิยม หรือโปรโมชั่น -> ไปหน้า `/products?recommended=true` หรือ `/products?sale=true`
  - **ประวัติการสั่งซื้อ (Orders):** ไปหน้าติดตามสถานะ (`/myorders`) **[ต้องล็อกอินก่อน]**
- **ไอคอนและการโต้ตอบ (Icons & Interaction):**
  - **Search:** เปิดช่องค้นหาเพื่อพิมพ์ชื่อสินค้า (ค้นหาแบบ Real-time)
  - **Wishlist:** ไปหน้าสินค้าที่ถูกใจ (`/wishlist`) 
    - *ลอจิก:* ถ้ายังไม่ล็อกอิน -> แสดง Popup เตือนให้ Login
  - **Cart:** ไปหน้าตะกร้าสินค้า (`/cart`)
    - *ลอจิก:* ถ้ายังไม่ล็อกอิน -> แสดง Popup เตือนให้ Login (Member Only Policy)
  - **User Menu:** แสดงชื่อผู้ใช้ (เช่น momo) เมื่อคลิกจะเปิดเมนูไปหน้า **Profile** หรือ **Logout**
  - **Language Switcher:** กดเพื่อสลับภาษา (TH/EN) ระบบจะเปลี่ยนคำแปลทั้งหน้าทันทีด้วย React Context
- **การเชื่อมต่อฐานข้อมูล & API:** ดึงข้อมูล `username` จากตาราง `users`, จำนวนของจากตาราง `cart` และ `wishlist` ผ่าน API `GET /api/cart/count`
- **ตัวอย่างการทำงาน (Execution Example):** 
  - *สถานการณ์:* ผู้ใช้พิมพ์คำว่า "Air" ในช่อง Search
  - *ผลลัพธ์:* ระบบยิง API ไปหาคำว่า "Air" ในชื่อและรายละเอียดสินค้า จากนั้นจะแสดงรายการสินค้าเช่น *Nike Air Jordan, Air Max* ขึ้นมาให้เลือกทันที

---

## 1. การทำงานฝั่งผู้ใช้งานทั่วไป (User / Customer Portal)
*(โดเมนการใช้งาน: สมัครสมาชิก, เลือกซื้อ, ติดตามสถานะ, รายการโปรด และคอมเมนต์รีวิว)*

### 1.1 ระบบยืนยันตัวตนและการจัดการสมาชิก (Authentication & User Registration)
หน้านี้คือประตูหน้าบ้าน (Entry Point) ที่ใช้จัดการสิทธิ์ผู้ใช้งานทั้งหมด โดยมีการออกแบบเน้นความสวยงามระดับพรีเมียม (Premium Modern UI)

- **การออกแบบประสบการณ์ผู้ใช้ (UX/UI Design Highlights):**
  - **Minimalist Card Design:** การใช้ Layout แบบ Card ที่มีการแบ่งพื้นที่เป็นส่วนของแบรนด์ (Velin Logo) และส่วนของแบบฟอร์มอย่างสมดุล (Dark Navy Contrast)
  - **Dynamic Transition:** ระบบสามารถสลับไปมาระหว่างหน้า "Sign In" และ "Register" ได้อย่างลื่นไหลด้วยเทคโนโลยี Single Page Application (SPA)
  - **Interactive Form Fields:** มีการใช้ไอคอนสื่อความหมายในช่อง Input และระบบ **Password Visibility Toggle** (ไอคอนดวงตา) เพื่อความสะดวกในการใช้งาน

- **ลอจิกการทำงานและความปลอดภัย (Logic & Security):** 
  - **Registration Logic:** ตรวจสอบความซ้ำซ้อนของข้อมูล (Unique Email/Username) และตรวจสอบความตรงกันของรหัสผ่าน (Confirm Password Validation)
  - **Password Hashing:** ระบบจะไม่เก็บรหัสผ่านจริง (Plain Text) แต่จะทำการ Salt และ Hash ผ่านอัลกอริทึม Bcrypt ก่อนบันทึกลงฐานข้อมูลเสมอ
  - **Token-Based Authentication:** หลังล็อกอินสำเร็จ ระบบจะออกใบเบิกทางดิจิทัล (JSON Web Token - JWT) เพื่อใช้ในการยืนยันสิทธิ์สำหรับคำสั่งซื้อหรือการเข้าถึงข้อมูลส่วนตัว

- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `POST /api/auth/register` (สมัครสมาชิก), `POST /api/auth/login` (เข้าสู่ระบบ)
  - **Database:** เชื่อมโยงกับตาราง **`users`** (เก็บข้อมูล ID, Username, Email, Password, Role)
- **ผลลัพธ์หลังการกดปุ่ม (Post-Login Workflow):**
  - กรณีสำเร็จ (Success):
    - ระบบบันทึก JWT Token ลง LocalStorage
    - **User:** นำทางไปหน้า Home (`/`) และทำการ Sync ตะกร้าสินค้าจาก Guest มาเป็นของสมาชิก
    - **Admin:** นำทางไปหน้าระบบจัดการสินค้า (`/admin`)
    - **Super Admin:** แสดง Popup ตัวเลือกการทำงาน (จัดการสินค้า หรือ สแกนใบหน้าเพื่อจัดการแอดมิน)
  - กรณีล้มเหลว (Failed):
    - แสดงแถบ Alert แจ้งเตือนข้อผิดพลาด (เช่น รหัสผ่านไม่ถูกต้อง)
    - ระบบจะค้างอยู่ที่หน้าเดิมและล้างช่องรหัสผ่าน เพื่อให้ผู้ใช้กรอกใหม่โดยไม่ต้องเปลี่ยนหน้า

### 1.2 หน้าแรก (Home Page)
หน้านี้เปรียบเสมือน "โชว์รูม" สินค้าที่เน้นความสวยงามและการนำเสนอสินค้าเด่น
- **ฟีเจอร์ & การโต้ตอบ (Features & Click Logic):**
  - **3D Cube Promo Slider:** แสดงสินค้าไฮไลท์
    - *การโต้ตอบ:* เมื่อกดที่ภาพบนลูกบาศก์ -> ระบบจะพาไปยังหน้า **Product Detail** (`/product/:id`) ของชิ้นนั้นทันที
  - **Quick Action Button:** ปุ่ม "ซื้อเลย" (Buy Now) 
    - *ลอจิก:* หากยังไม่ล็อกอิน -> แสดง Popup เตือน | หากล็อกอินแล้ว -> พาไปหน้าสินค้าเพื่อเลือกไซส์
  - **Product Showcases:** หมวดสินค้ามาใหม่ (New Arrivals)
    - *การโต้ตอบ:* กดที่รูปสินค้าเพื่อดูรายละเอียดเชิงลึก
- **การเชื่อมต่อฐานข้อมูล & API:** ดึงข้อมูลจากตาราง **`products`** (Filter เฉพาะ `is_featured = 1`) ผ่าน API `GET /api/products?recommended=true`

### 1.3 หน้าสินค้าทั้งหมด (All Products Page)
หน้านี้คือหน้าร้านค้าหลัก (Shop) สำหรับเลือกชมสินค้าแบบละเอียด
- **ฟีเจอร์ & การโต้ตอบ (Features & Click Logic):**
  - **Brand Filter Bar:** โลโก้แบรนด์ต่างๆ
    - *การโต้ตอบ:* เมื่อกดโลโก้ (เช่น Nike) -> ระบบจะกรองรายการสินค้าในหน้าจอให้เหลือเฉพาะแบรนด์นั้นทันที (API: `GET /api/products?brand=nike`)
  - **Product Grid:** การ์ดรายการสินค้า
    - *การโต้ตอบ:* กดที่ตัวการ์ดเพื่อไปหน้ารายละเอียดสินค้า (`/product/:id`)
  - **Wishlist Toggle (ปุ่มหัวใจ):** 
    - *ลอจิก:* หากยังไม่ล็อกอิน -> แสดง Popup เตือนให้ Login | หากล็อกอินแล้ว -> หัวใจเปลี่ยนสีและบันทึกลงตาราง `wishlist` ทันที
- **การเชื่อมต่อฐานข้อมูล & API:** ดึงข้อมูลจากตาราง **`products`** และตรวจสอบสถานะในตาราง **`wishlist`**
- **ตัวอย่างการทำงาน (Execution Example):**
  - *สถานการณ์:* ผู้ใช้กดที่โลโก้ **"Nike"** บนแถบ Filter
  - *ผลลัพธ์ที่เกิดขึ้นทันที (3 ขั้นตอน):*
    1. **URL Update:** แถบ URL เปลี่ยนเป็น `/products?brand=nike` (รองรับการแชร์ลิงก์เฉพาะแบรนด์)
    2. **Counter Update:** ตัวเลข "พบสินค้า X รายการ" จะอัปเดตยอดตามจำนวนสินค้าของ Nike ที่มีจริงในขณะนั้น
    3. **Grid Refresh:** รายการสินค้าในหน้าจอจะถูกกรองใหม่ (Filter) ให้เหลือเฉพาะแบรนด์ Nike โดยไม่ต้องโหลดหน้าเว็บใหม่ทั้งหมด (Single Page Application Logic)


### 1.4 หน้ารายละเอียดสินค้า (Product Detail Page)
หน้านี้คือส่วนที่ให้ข้อมูลสินค้าเชิงลึกเพื่อการตัดสินใจซื้อ
- **ส่วนประกอบของหน้า (UI Components):**
  - **Product Info:** แสดงแบรนด์ (Nike), รหัสสินค้า (Code), ชื่อรุ่น, หมวดหมู่ และราคาสุทธิ
  - **Availability Status:** ป้ายบอกสถานะ "มีสินค้า" หรือ "สินค้าหมด" แบบ Dynamic
  - **Image Gallery:** ระบบรูปภาพที่ประกอบด้วยภาพหลักและรูปย่อย (Thumbnails) พร้อมปุ่มลูกศรเลื่อนดูมุมต่างๆ
  - **Size Selector Grid:** ตารางแสดงไซส์ที่พร้อมส่ง (ดึงจากสต็อกจริง) เช่น 38.5, 39, 41, 42
  - **Reviews & Ratings:** แสดงคะแนนเฉลี่ย (ดาว) และจำนวนรีวิว พร้อมประวัติคอมเมนต์ที่มีชื่อผู้ใช้, ข้อความรีวิว และ **"วันที่/เวลาที่รีวิว"**
- **การโต้ตอบ & ลอจิก (Interaction Logic):**
  - **การเลือกไซส์:** ผู้ใช้ต้องคลิกเลือกไซส์ที่ต้องการก่อน ปุ่ม "เพิ่มลงตะกร้า" ถึงจะทำงาน
  - **Add to Cart:** 
    - *กรณีไม่ล็อกอิน:* แสดง Popup เตือนให้เข้าสู่ระบบ
    - *กรณีล็อกอิน:* ส่งข้อมูล {productId, size, quantity} ไปยังตาราง `cart` พร้อมส่งสัญญาณ `window.dispatchEvent(new Event('cartUpdated'))` เพื่อให้ตัวเลขบน Navbar เพิ่มขึ้นทันทีโดยไม่ต้องรีเฟรชหน้าจอ
  - **Wishlist (ปุ่มหัวใจ):**
    - *การโต้ตอบ:* กดเพื่อบันทึกสินค้าที่ชอบ
    - *ลอจิก:* หากล็อกอินแล้ว ปุ่มจะเปลี่ยนเป็น **สีแดง** และบันทึกลงตาราง `wishlist` | หากกดซ้ำจะเป็นการยกเลิก (เปลี่ยนกลับเป็นสีขาว)
    - *กรณีไม่ล็อกอิน:* แสดง Popup เตือนให้ Login เช่นเดียวกับการกดซื้อ
- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `GET /api/products/:id` (ดึงข้อมูลสินค้าและไซส์) และ `GET /api/comments/:id` (ดึงรีวิว)
  - **Database:** ตาราง `products`, `product_sizes`, `product_comments`

  - **Database:** ตาราง `products`, `product_sizes`, `product_comments`

### 1.5 ตะกร้าสินค้า (Shopping Cart Page `/cart`)
หน้าสำหรับตรวจสอบความถูกต้อง ปรับปรุงจำนวน และคำนวณราคาสุทธิก่อนเข้าสู่กระบวนการชำระเงิน

- **ส่วนประกอบหน้าจอเชิงลึก (Detailed UI Components):**
  - **Cart Item Card:** แสดงภาพสินค้าขนาด Thumbnail พร้อมรายละเอียดชื่อรุ่น และไซส์ที่เลือกไว้อย่างชัดเจน
  - **Interactive Quantity Controls:** ปุ่มเพิ่ม/ลดจำนวนแบบโต้ตอบ โดยระบบจะล็อกขั้นต่ำไว้ที่ 1 ชิ้น และมีปุ่ม "ถังขยะ" (สีแดง) สำหรับการลบสินค้าออกจากรายการทันที
  - **Shipping Method Selector:** แผงเลือกประเภทการจัดส่ง (EMS ด่วน หรือ แมสเซนเจอร์) ที่มีการออกแบบเป็น Badge แสดงสถานะการเลือก (Active State)
  - **Dynamic Order Summary:** ส่วนสรุปยอดเงินที่แสดง ยอดรวมสินค้า, ค่าจัดส่ง และยอดสุทธิ (Total) ที่เน้นด้วยตัวอักษรสีเขียวขนาดใหญ่ เพื่อความชัดเจนในการสื่อสารข้อมูลราคา

- **ลอจิกการทำงานและเทคโนโลยี (Business Logic & Technology):**
  - **Real-time Recalculation:** ทุกครั้งที่มีการกดปรับจำนวนหรือเปลี่ยนประเภทการส่ง ระบบจะใช้เทคโนโลยี Single Page Application (SPA) ในการคำนวณยอดรวมใหม่ทันทีโดยไม่ต้องรีเฟรชหน้าจอ (Non-blocking UI)
  - **Shipping Fee Logic:** 
    - ระบบจะตรวจสอบที่อยู่ผู้ใช้จากฐานข้อมูล หากเป็นพื้นที่กรุงเทพฯ จะเปิดให้เลือก "แมสเซนเจอร์ (60 บาท)"
    - หากเป็นพื้นที่อื่นๆ หรือผู้ใช้เลือกแบบด่วน จะคำนวณค่าส่ง "EMS (150 บาท)"
  - **Data Synchronization:** การทำงานเชื่อมโยงกับ LocalStorage (สำหรับ Guest) และ Database (สำหรับ Member) เพื่อรักษาข้อมูลสินค้าในตะกร้าให้คงอยู่แม้ปิดบราวเซอร์
  - **Checkout Guard:** ปุ่ม "ดำเนินการชำระเงิน" จะมีการตรวจสอบความพร้อมของสต็อกสินค้าและสถานะที่อยู่ก่อนนำทางผู้ใช้ไปสู่หน้าชำระเงิน (Payment Gateway)

- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `GET /api/cart` (ดึงรายการ), `PUT /api/cart/:id` (อัปเดตจำนวน), `DELETE /api/cart/:id` (ลบรายการ)
  - **Database:** ตาราง `cart` (เก็บข้อมูลชั่วคราว) และ `products` (ดึงข้อมูลราคาและสต็อกล่าสุด)

### 1.6 หน้าชำระเงิน (Payment Page `/payment`)
ส่วนงานที่สำคัญที่สุดในกระบวนการ E-commerce สำหรับการสรุปยอด รวบรวมข้อมูลจัดส่ง และเลือกช่องทางชำระเงิน

- **ส่วนประกอบหน้าจอระดับมืออาชีพ (Professional UI Elements):**
  - **Multi-Channel Payment Selector:** ตัวเลือกการชำระเงินที่หลากหลาย (ธนาคาร, พร้อมเพย์, บัตรเครดิต) โดยมีการใช้สถานะ Active (กรอบสีเขียวและเครื่องหมายถูก) เพื่อระบุช่องทางที่เลือก
  - **Bank Details Card:** แสดงข้อมูลบัญชีธนาคาร (SCB, K-Bank, KTB) พร้อมโลโก้ เลขบัญชี และชื่อบัญชีบริษัท เพื่อความสะดวกและลดความผิดพลาดในการโอนเงิน
  - **Summary & Logistics Sidebar:** แถบด้านข้างที่สรุปข้อมูลสำคัญทั้งหมดไว้ในที่เดียว ประกอบด้วย:
    - **Product Recap:** รายการสินค้าพร้อม Badge ระบุจำนวนสินค้า และไซส์ที่เลือก
    - **Live Shipping Address:** แสดงชื่อ เบอร์โทร และที่อยู่จัดส่งปัจจุบัน พร้อมปุ่ม "แก้ไข" สำหรับปรับปรุงข้อมูลทันที
    - **Shipping Method Selector:** เลือกการขนส่ง (EMS/Messenger) พร้อมแสดงราคาบวกเพิ่มแบบ Real-time
    - **Final Total Amount:** ยอดรวมสุดท้ายที่เน้นด้วยสีเขียวเข้มและขนาดตัวอักษรที่ชัดเจน

- **ลอจิกเบื้องหลังและการจัดการข้อมูล (Backend Logic & Data Integrity):**
  - **Address Snapshot Technology:** ระบบจะทำ "สำเนา" (Snapshot) ของที่อยู่จัดส่งและราคา ณ วินาทีที่ผู้ใช้กด "ยืนยันการชำระเงิน" ลงในตาราง `orders` โดยตรง เพื่อป้องกันปัญหาข้อมูลในใบเสร็จเปลี่ยนตามโปรไฟล์หากมีการแก้ไขที่อยู่ภายหลัง
  - **Multi-part Form Submission:** การส่งข้อมูลคำสั่งซื้อจะรวมทั้งข้อมูล JSON (รายการสินค้า, ที่อยู่) และไฟล์ภาพ (สลิปโอนเงิน) ผ่านเทคโนโลยี Multer บน Node.js
  - **Order State Management:** เมื่อเริ่มเข้าสู่หน้านี้ ระบบจะสร้างสถานะคำสั่งซื้อเป็น `Pending` (รอดำเนินการ) และเตรียมการหักสต็อกในระดับฐานข้อมูล (Optimistic Locking)

- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `POST /api/orders` (สร้างคำสั่งซื้อใหม่), `GET /api/auth/profile` (ดึงข้อมูลที่อยู่เริ่มต้น)
  - **Database:** ตาราง `orders` (เก็บหัวกระดาษ), `order_items` (เก็บรายการสินค้า), `shipping_addresses` (ดึงที่อยู่)
  - **Inventory Update:** หักสต็อกสินค้าออกจากระบบ และเคลียร์รายการในตะกร้า (`cart_items`) ของผู้ใช้ทิ้งทันทีหลังทำรายการสำเร็จ

### 1.7 หน้าประวัติการสั่งซื้อและติดตามสถานะ (My Orders `/myorders`)
ศูนย์กลางการติดตามสถานะสินค้าและดูประวัติการทำรายการย้อนหลังทั้งหมดของผู้ใช้

- **โครงสร้างการแสดงผลแบบการ์ด (Order Card Architecture):**
  - **Header Section:** แสดงหมายเลขคำสั่งซื้อ (Order ID เช่น #16) พร้อมระบุ วันที่และเวลา ที่ทำรายการอย่างแม่นยำ และมีป้ายสถานะ (Status Badge) ที่ใช้สีสื่อความหมาย เช่น สีส้มสำหรับ "รอดำเนินการ"
  - **Product List Section:** แสดงรายการสินค้าแต่ละชิ้นในออเดอร์นั้นๆ พร้อมรูปภาพ, ไซส์, จำนวน และราคาต่อหน่วย เพื่อให้ผู้ใช้ตรวจสอบความถูกต้องของรายการได้ง่าย
  - **Summary Footer:** แสดงยอดราคาสุทธิ (Grand Total) ที่เน้นด้วยตัวเลขสีเขียวขนาดใหญ่ ซึ่งเป็นยอดที่รวมค่าจัดส่งเรียบร้อยแล้ว

- **ลอจิกการทำงานและสถานะคำสั่งซื้อ (Order Lifecycle Logic):**
  - **Personalized Data Fetching:** ระบบจะกรองและดึงข้อมูลออเดอร์เฉพาะของ `user_id` ที่ล็อกอินอยู่เท่านั้น โดยเรียงลำดับจากออเดอร์ล่าสุดไว้ด้านบน (Sort by Newest)
  - **Dynamic Status Tracking:** ระบบจะอัปเดตสถานะแบบ Real-time ตามการดำเนินการของแอดมิน:
    - `Pending` (รอดำเนินการ): สีส้ม - อยู่ในขั้นตอนรอแอดมินตรวจสอบสลิป
    - `Shipped` (กำลังจัดส่ง): สีฟ้า - สินค้าถูกส่งออกเรียบร้อยแล้ว
    - `Arrived` (ถึงปลายทาง): สีม่วง - สินค้าถึงมือผู้รับ รอการยืนยัน
    - `Delivered` (สำเร็จ): สีเขียว - ผู้ใช้กดยืนยันการรับสินค้าเรียบร้อย
  - **Interactive Actions:** เมื่อสถานะเป็น `Arrived` ระบบจะปรากฏปุ่ม **"ฉันได้รับสินค้าแล้ว"** เพื่อให้ผู้ใช้เปลี่ยนสถานะเป็น `Delivered` ด้วยตนเอง

- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `GET /api/orders/my` (ดึงประวัติส่วนตัว), `PUT /api/orders/:id/deliver` (ยืนยันรับสินค้า)
  - **Database:** ตาราง `orders` เชื่อมโยง (Join) กับ `order_items` และ `products` เพื่อดึงข้อมูลสินค้าที่สั่งซื้อไปแสดงผลครบถ้วน

#### 1.7.1 ตารางวงจรการปฏิสัมพันธ์ระหว่างผู้ซื้อและผู้ดูแลระบบ (Order Interaction Flow)
ตารางสรุปการทำงานข้ามหน้าจอเพื่อให้เห็นภาพการโต้ตอบ (Interaction) ของระบบ:

| ขั้นตอน | การกระทำของผู้ใช้ (User Action) | สถานะที่ปรากฏ (System Status) | การกระทำของผู้ดูแลระบบ (Admin Action) |
| :--- | :--- | :--- | :--- |
| **1. สั่งซื้อ** | เลือกสินค้าและ **อัปโหลดสลิป** | `Pending` (สีส้ม) | ตรวจสอบภาพสลิปในหน้า Manage Orders |
| **2. ยืนยันเงิน** | รอการตรวจสอบ | `Verified` / `Processing` | กดปุ่ม **Verify Payment** (ตรวจสอบสลิปผ่าน) |
| **3. การจัดส่ง** | รอรับสินค้า | `Shipped` (สีฟ้า) | กดปุ่ม **Ship (รถบรรทุก)** เพื่อยืนยันการส่งออก |
| **4. สินค้าถึง** | สินค้ามาถึงมือผู้รับ | `Arrived` (สีม่วง) | เปลี่ยนสถานะเป็น Arrived เมื่อขนส่งยืนยัน |
| **5. รับสินค้า** | กดปุ่ม **"ฉันได้รับสินค้าแล้ว"** | `Delivered` (สีเขียว) | (ระบบบันทึกสถานะโดยอัตโนมัติ) |
| **6. รีวิวสินค้า** | **ให้คะแนนดาวและพิมพ์รีวิว** | `Reviewed` (ดาวทอง) | (ระบบแสดงผลรีวิวที่หน้า Product Detail) |

#### 1.7.2 การจัดการกรณีเกิดปัญหาและการคืนเงิน (Error Handling & Refund Management)
ระบบมีกลไกรับรองความผิดพลาดเพื่อรักษาผลประโยชน์ของทั้งผู้ซื้อและผู้ขาย:
- **กรณีสลิปไม่ถูกต้อง (Invalid/Incorrect Slip):**
  - แอดมินจะกด **"ยกเลิก (Cancel)"** ออเดอร์นั้น
  - **ฝั่งผู้ใช้:** สถานะจะเปลี่ยนเป็น `Cancelled` (สีแดง) และจะปรากฏปุ่ม **"Provide Refund Info"** ให้ผู้ใช้กรอกชื่อธนาคารและเลขบัญชีเพื่อขอเงินคืน
- **ระบบการคืนเงิน (Refund Process):**
  - เมื่อแอดมินได้รับข้อมูลบัญชีแล้ว จะทำการโอนเงินคืนและกดปุ่ม **"Confirm Refund"** พร้อม **อัปโหลดสลิปหลักฐานการโอนเงินคืน** เข้าสู่ระบบ
  - **ฝั่งผู้ใช้:** สถานะจะเปลี่ยนเป็น `Refunded` (สีเขียวอ่อน) และจะปรากฏปุ่ม **"ดูหลักฐานการคืนเงิน"** เพื่อให้ลูกค้าคลิกเปิดดูภาพสลิปที่แอดมินแนบมาได้ทันที เพื่อความโปร่งใสและตรวจสอบได้
- **การคืนสต็อกสินค้าอัตโนมัติ (Automatic Inventory Restoration):**
  - ระบบ Backend จะทำการบวกจำนวนสินค้ากลับเข้าสู่คลังทันทีที่ออเดอร์ถูก Cancel หรือ Refund เพื่อให้สินค้าพร้อมขายใหม่ทันที

#### 1.7.3 ระบบการรีวิวและให้คะแนน (Product Review & Rating System)
ขั้นตอนสุดท้ายเพื่อสร้างความน่าเชื่อถือ (Social Proof) ให้กับสินค้าในระบบ:
- **Review Trigger:** ปุ่ม **"เขียนรีวิว (Write Review)"** จะปรากฏขึ้นเฉพาะออเดอร์ที่มีสถานะเป็น `Delivered` เท่านั้น เพื่อป้องกันการรีวิวปลอมจากผู้ที่ไม่ได้ซื้อสินค้าจริง
- **Interactive Review Modal:** 
  - **Rating:** ผู้ใช้สามารถให้คะแนนได้ 1-5 ดาว (Interactive Star Rating)
  - **Comment:** พื้นที่สำหรับพิมพ์ข้อความแสดงความคิดเห็น
- **Impact on Store:** เมื่อบันทึกรีวิวแล้ว คะแนนเฉลี่ยและข้อความรีวิวจะไปปรากฏที่หน้า **Product Detail** ของสินค้านั้นๆ ทันที ทำให้ลูกค้าคนอื่นใช้ประกอบการตัดสินใจซื้อได้

### 1.8 หน้ารายการโปรด (Wishlist Page ` `/wishlist` `)
หน้ารวบรวมสินค้าที่ผู้ใช้สนใจเพื่อกลับมาเลือกซื้อภายหลัง
- **ส่วนประกอบหน้าจอ (UI Elements):**
  - **Wishlist Counter:** แสดงจำนวนรวมของสินค้าที่บันทึกไว้ เช่น "รายการโปรด (1)"
  - **Saved Product Card:** แสดงรูปภาพ, ชื่อ และราคาของสินค้าที่บันทึกไว้
  - **Quick Remove:** ปุ่มหัวใจสีแดงบนภาพสินค้าที่สามารถกดเพื่อนำออกจากรายการโปรดได้ทันที
  - **View Detail Button:** ปุ่มทางลัดพาผู้ใช้กลับไปยังหน้า `/product/:id` เพื่อทำการเลือกไซส์และสั่งซื้อ
- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `GET /api/wishlist` (ดึงรายการทั้งหมดของผู้ใช้คนนั้น)
  - **Database:** ตาราง `wishlist` เชื่อมโยง (Join) กับตาราง `products` เพื่อแสดงข้อมูลสินค้าล่าสุด

### 1.9 การจัดการข้อมูลส่วนตัว (Profile & Account Settings ` `/profile` `)
หน้าสำหรับจัดการข้อมูลระบุตัวตนและที่อยู่จัดส่งของผู้ใช้
- **ส่วนประกอบหน้าจอ (UI Elements):**
  - **Profile Avatar:** แสดงรูปโปรไฟล์ปัจจุบัน และปุ่มอัปโหลดรูปภาพใหม่ผ่านระบบ Multer หลังบ้าน
  - **Account Details:** ฟอร์มแสดง Username และ Email ที่ไม่สามารถแก้ไขได้ (เพื่อความปลอดภัยทางบัญชี)
  - **Security Actions (Change Password Modal):** 
    - *ฟิลด์ข้อมูล:* รหัสผ่านปัจจุบัน, รหัสผ่านใหม่, และยืนยันรหัสผ่านใหม่
    - *ลอจิกการทำงาน:* ระบบจะส่งรหัสปัจจุบันไปให้ Backend ตรวจสอบด้วย `Bcrypt.compare` หากถูกต้องจึงจะอนุญาตให้ Hash รหัสใหม่บันทึกลงฐานข้อมูล
  - **Shipping Info Section (Manage Address):** 
    - *รายละเอียดฟอร์ม:* ชื่อ-นามสกุล, เบอร์โทรศัพท์, ที่อยู่โดยละเอียด (บ้านเลขที่/หมู่บ้าน), ตำบล/แขวง, อำเภอ/เขต, จังหวัด และรหัสไปรษณีย์
    - *ระบบแผนที่ (Map Locator Technology):* พัฒนาด้วย **React-Leaflet** โดยดึงข้อมูลภาพแผนที่แบบ Real-time จาก **Tile Server ของ OpenStreetMap (OSM)** ทำให้แสดงผลแผนที่โลกได้ละเอียดโดยไม่หนักเครื่องแม่ข่าย
    - *ข้อมูลที่ได้รับและการจัดเก็บ:* ระบบจะบันทึกเฉพาะค่าพิกัดพอยเตอร์เป็นตัวเลขทศนิยม **Decimal (Latitude, Longitude)** ลงฐานข้อมูล เพื่อใช้ในการอ้างอิงตำแหน่งบนแผนที่ในภายหลัง
- **การเชื่อมต่อฐานข้อมูล & API:**
  - **API:** `GET /api/users/me` (ดึงข้อมูลส่วนตัว), `PUT /api/users/change-password` (เปลี่ยนรหัส), `POST /api/addresses` (เพิ่มที่อยู่)
  - **Database:** ตาราง `users` และ `shipping_addresses`

---

## 2. การทำงานฝั่งผู้ดูแลระบบ (Admin Portal)
*(โดเมนการใช้งาน: อนุมัติสลิป, จัดส่ง, พิมพ์เอกสาร, และอัปเดตสต๊อก)*

**ข้อกำหนดสิทธิ์ (Admin Level Permissions):** 
- เข้าใช้งานด้วยระบบ Username/Password ปกติ
- **ข้อจำกัด:** ไม่สามารถเข้าถึงเมนูการจัดการบุคลากร (Personnel) และไม่สามารถดูบันทึกกิจกรรมย้อนหลัง (System Logs) ของผู้อื่นได้

### 2.1 แถบเมนูนำทางหลัก (Admin Sidebar Navigation)
โครงสร้างการควบคุมระบบที่ออกแบบมาให้ใช้งานง่ายและเข้าถึงทุกส่วนได้รวดเร็ว (ตามภาพประกอบ Sidebar):
- **Brand Identity:** แสดงโลโก้ Velin พร้อมไอคอน "แม่กุญแจสีเขียว" เพื่อยืนยันว่าเป็นการเชื่อมต่อที่ปลอดภัย
- **Core Management Menu:**
  - **Dashboard:** หน้าสรุปภาพรวมทั้งหมด
  - **Products (Dropdown):** แยกการจัดการเป็นหน้ารายชื่อสินค้า (Products) และหน้าสรุปสต็อกคงคลัง (Inventory Summary)
  - **Orders:** ติดตามและอนุมัติรายการสั่งซื้อของลูกค้า
  - **Revenue & Best Sellers:** ส่วนงานวิเคราะห์การเงินและอันดับสินค้าขายดี
- **Admin Profile & Utility Section:**
  - **User Identity:** แสดงชื่อผู้ใช้งาน (เช่น worapol) พร้อมระบุตำแหน่ง (Super Admin) และรูปโปรไฟล์
  - **Language Toggle:** ระบบสลับภาษา (TH/EN) ที่รองรับการเปลี่ยนคำแปลทั้งระบบโดยไม่รีโหลดหน้า
  - **Logout Button:** ปุ่มออกจากระบบที่เด่นชัดเพื่อความปลอดภัยหลังเสร็จสิ้นการทำงาน

### 2.2 หน้าแดชบอร์ดภาพรวม (Dashboard Overview - New Version)
ศูนย์กลางการตัดสินใจที่ได้รับการอัปเดตใหม่เพื่อการติดตามสถานะที่แม่นยำยิ่งขึ้น โดยดึงข้อมูลจากตาราง **`orders`**, **`products`**, และ **`users`** มาสรุปผลดังนี้:

- **บัตรข้อมูลสรุปเบื้องต้น (Primary Metrics):**
  - **จัดการสินค้า (Products):** นับจำนวน Record ทั้งหมดจากตาราง `products` เพื่อแสดงขนาดของคลังสินค้าปัจจุบัน
  - **ลูกค้า (Customers):** นับจำนวนผู้ใช้งานในระบบที่มี `role = 'user'` เพื่อประเมินฐานลูกค้า

- **ส่วนงานวิเคราะห์สถานะออเดอร์ (Order Status Analysis - NEW):**
  - **ออเดอร์ทั้งหมดในระบบ (Total Orders):** แสดงปริมาณธุรกรรมทั้งหมดที่เคยเกิดขึ้นในระบบ (History Count)
  - **แถบสถานะความคืบหน้า (Status Progress Bars):**
    - **ออเดอร์ที่สำเร็จแล้ว (สีเขียว):** นับเฉพาะออเดอร์ที่มีสถานะ `delivered` เพื่อวัดผลงานการขายที่เสร็จสิ้นสมบูรณ์
    - **ออเดอร์เข้ามาใหม่/รอดำเนินการ (สีส้ม):** แจ้งเตือนจำนวนงานค้าง (Backlog) ที่แอดมินต้องรีบดำเนินการตรวจสอบสลิป
    - **ออเดอร์ที่จัดส่งแล้ว/ตกค้าง (สีฟ้า):** ติดตามพัสดุที่อยู่ระหว่างทาง (In Transit) เพื่อการันตีว่าถึงมือลูกค้าตามกำหนด

- **การวิเคราะห์ข้อมูลเชิงลึก (Advanced Analytics Widgets):**
  - **รายได้ พยากรณ์ 7 วันล่าสุด (Revenue Forecast):** 
    - *ลอจิก:* รวมยอดขายรายวันและใช้เทคนิคการพยากรณ์ (Forecasting) เพื่อให้แอดมินมองเห็นแนวโน้มทางการเงิน
    - *เทคโนโลยี:* ใช้ **Recharts (Responsive Container)** เพื่อให้กราฟปรับขนาดตามหน้าจอโดยอัตโนมัติ

- **เทคโนโลยีเบื้องหลัง (Technical Stack):**
  - **Backend API:** `GET /api/admin/dashboard/stats` (ดึงข้อมูลสรุปสถานะออเดอร์)
  - **Frontend State:** ใช้ **React Hooks (useEffect)** ในการเรียกข้อมูลใหม่ทุกครั้งที่แอดมินสลับกลับมาที่หน้า Dashboard เพื่อความสดใหม่ของตัวเลข (Freshness of Data)

### 2.3 ระบบจัดการคำสั่งซื้อลูกค้า (Advanced Order Management - `/admin/orders`)
หน้าจอควบคุมหลักสำหรับพนักงานฝ่ายขายและคลังสินค้า เพื่อกำกับดูแลวงจรชีวิตของออเดอร์ (Order Lifecycle) ตั้งแต่การตรวจเงินจนถึงการส่งมอบสินค้า:

- **ส่วนควบคุมและคัดกรองข้อมูล (Advanced Control & Filters):**
  - **Status Overview Chips:** แถบไอคอนด้านบนที่แสดงจำนวนออเดอร์แบบ Real-time แบ่งตามสถานะ (Pending, Shipped, Arrived...) ช่วยให้แอดมินประเมินปริมาณงานที่ต้องจัดการได้ทันที
  - **Dual-Layer Filter System:**
    - **Payment Verification Filter:** แยกออเดอร์ตามการชำระเงิน ได้แก่ `All Orders`, `Pending Check` (รอตรวจสลิป), และ `Verified Only` (ตรวจผ่านแล้ว) เพื่อป้องกันการส่งของให้ลูกค้าที่ยังไม่โอนเงิน
    - **Shipping Status Filter:** คัดกรองออเดอร์ตามขั้นตอนขนส่ง (7 สถานะ) เพื่อความแม่นยำในการจัดเตรียมพัสดุ
  
- **ระบบตารางจัดการข้อมูล (Order Intelligence Table):**
  - **Status-Based Color Coding:** พื้นหลังของแถวข้อมูลจะเปลี่ยนสีตามสถานะเพื่อการรับรู้ที่รวดเร็ว:
    - **แถบสีเหลือง:** ออเดอร์ใหม่สถานะ `Pending` (รอดำเนินการ) ที่ต้องตรวจสอบความถูกต้องเร่งด่วน
    - **แถบสีเขียว:** ออเดอร์สถานะ `Completed` ที่ดำเนินการเสร็จสิ้นสมบูรณ์
    - **แถบสีเขียวอ่อน:** ออเดอร์ที่ถูก `Refunded` หรือมีการคืนเงินสำเร็จพร้อมแนบสลิปหลักฐาน
  - **Payment Verification Column:** 
    - **ไอคอนเอกสาร (Blue Document):** ปุ่มสำหรับเปิดดู "หลักฐานการโอนเงิน (Slip)" ที่ลูกค้าอัปโหลดมาตอนซื้อสินค้า
    - **ไอคอนหลักฐานคืนเงิน (Green Slip):** สำหรับออเดอร์ที่คืนเงินแล้ว แอดมินสามารถกดดูสลิปที่ตัวเองอัปโหลดไปได้เช่นกัน
    - **Checkmark/Warning Icon:** สัญลักษณ์ระบุว่าสลิปนี้ได้รับการยืนยันความถูกต้องจากแอดมินแล้วหรือยัง
  
- **ชุดคำสั่งจัดการคำสั่งซื้อ (Action Matrix):**
  - **Button Action Logic:** ระบบจะมีปุ่มตอบสนองตามสถานะปัจจุบันของออเดอร์:
    - **ปุ่มกากบาทสีแดง (Cancel):** สำหรับยกเลิกออเดอร์ในกรณีสินค้ามีปัญหาหรือสลิปปลอม
    - **ปุ่มคืนเงิน (Refund):** ปรากฏเมื่อออเดอร์ถูกยกเลิกและลูกค้าส่งเลขบัญชีมาแล้ว แอดมินต้องอัปโหลดสลิปคืนเงินเพื่อปิดงาน
    - **ปุ่มรถบรรทุกสีฟ้า (Ship):** สำหรับกดยืนยันว่าส่งสินค้าออกไปแล้ว (จะกดได้เฉพาะออเดอร์ที่ Verified เงินแล้วเท่านั้น)
    - **สถานะ Finalized/Cancelled:** เมื่อออเดอร์จบกระบวนการ ระบบจะแสดงข้อความสรุปแทนปุ่มกด เพื่อป้องกันการแก้ไขข้อมูลย้อนหลังโดยไม่ได้รับอนุญาต
  - **System Logs Integration:** ทุกการเปลี่ยนสถานะจะถูกบันทึกร่องรอยการทำงาน (Activity Logs) เพื่อการตรวจสอบย้อนหลัง (Audit Trail)
  - **Data Integrity Policy (สำคัญมาก):** ในหน้านี้จะ **"ไม่มีปุ่มลบ (Delete)"** ออเดอร์ทิ้งอย่างเด็ดขาด เพื่อป้องกันการทุจริตและการแต่งตัวเลขบัญชี หากออเดอร์มีปัญหาต้องใช้การ `Cancel` หรือ `Refund` เท่านั้น เพื่อให้ระบบยังคงมีประวัติการทำรายการ (Transaction History) ไว้ตรวจสอบย้อนหลังได้ 100%

- **การติดตามผลการรับสินค้า (How Admin knows User received items):**
  - แอดมินไม่ต้องโทรเช็คกับลูกค้า เพราะระบบใช้ **"User Confirmation Logic"**
  - เมื่อแอดมินเปลี่ยนสถานะเป็น `Arrived` แล้ว แอดมินเพียงแค่รอสังเกตในตารางจัดการออเดอร์
  - หากสถานะในตารางเปลี่ยนจากสีม่วงเป็น **"สีเขียว (Delivered)"** แสดงว่าลูกค้ายืนยันการรับสินค้าผ่านหน้าจอ My Orders เรียบร้อยแล้ว แอดมินจึงจะถือว่าออเดอร์นี้ "จบงานสมบูรณ์" และระบบจะนำยอดเงินนี้ไปคำนวณในรายงานสรุปรายได้จริง

  - **API:** `GET /api/admin/orders` (ดึงออเดอร์ทั้งหมด), `PUT /api/admin/orders/:id/status` (อัปเดตสถานะ), `PUT /api/admin/orders/:id/verify-payment` (ยืนยันสลิป)
  - **Database Update:** อัปเดตฟิลด์ `status` และ `payment_status` ในตาราง **`orders`** ซึ่งจะส่งผลให้หน้าจอติดตามสถานะของฝั่งลูกค้าอัปเดตตามทันที

### 2.4 การจัดการสินค้าแบบเจาะลึก (Products Management - `/admin/products`)
หน้าต่างควบคุมคลังสินค้าที่ถูกออกแบบมาให้แสดงข้อมูลครบถ้วนในหน้าเดียว (Single-Page Inventory Grid) ช่วยให้แอดมินมองเห็นภาพรวมของสต็อกและราคาสินค้าทั้งหมด

- **ระบบค้นหาและคัดกรองอัจฉริยะ (Smart Search & Filtering):**
  - **Quick Alerts:** ปุ่มทางลัดสีเหลืองสำหรับกรองสินค้าที่มีปัญหาทันที ได้แก่ `สต็อก < 5` (สต็อกรวมใกล้หมด) และ `ไซส์ < 5` (มีบางไซส์ขาดสต็อก)
  - **Advanced Search Syntax (ระบบค้นหาด้วยคีย์ลับ):** นอกจากการพิมพ์ชื่อสินค้าปกติแล้ว แอดมินสามารถใช้ "คำสั่งลับ" นำหน้าเพื่อค้นหาเจาะจงฟิลด์ได้:
    - `code: [รหัสสินค้า]` - ค้นหาเจาะจงเฉพาะ SKU (เช่น `code: TSAJ-1309`)
    - `brand: [ชื่อแบรนด์]` - ค้นหาเฉพาะแบรนด์ (เช่น `brand: Nike`)
    - `price: [ตัวเลข]` - ค้นหาสินค้าที่มีราคาตรงกับที่ระบุ
    - `stock: [ตัวเลข]` - ค้นหาสินค้าที่มีจำนวนสต็อกรวมตรงกับที่ระบุ
    - *Default:* หากไม่ใส่คีย์ลับ ระบบจะค้นหาจาก **ชื่อสินค้า (Name)** และ **รายละเอียด (Description)**
  - **Add Product:** ปุ่มสีม่วง (`+ เพิ่มสินค้า`) สำหรับสร้างรายการสินค้าใหม่เข้าสู่ระบบ ซึ่งจะเปิดหน้าต่าง Modal ขนาดใหญ่ขึ้นมา

- **หน้าต่างจัดการข้อมูลสินค้า (Add / Edit Product Modal):**
  เมื่อแอดมินกดเพิ่มหรือแก้ไขสินค้า ระบบจะแสดงฟอร์มบันทึกข้อมูลที่แบ่งเป็น 2 ฝั่งหลักเพื่อความรวดเร็วในการกรอก:
  - **ฝั่งซ้าย (ข้อมูลหลักสินค้า):**
    - `ชื่อสินค้า` และ `รายละเอียด`: สำหรับการแสดงผลให้ลูกค้าเห็น
    - `รหัสสินค้า (SKU)`: คีย์อ้างอิงของสินค้า (หากไม่กรอก ระบบจะสร้าง ID ให้อัตโนมัติหลังเซฟ)
    - `ราคา` และ `ส่วนลด (%)`: สำหรับให้ระบบ Pricing Engine นำไปคำนวณราคาสุทธิ
    - `หมวดหมู่` และ `แบรนด์`: สำหรับจัดกลุ่มสินค้า
    - `จัดการคลังรูปภาพ (Gallery)`: รองรับการอัปโหลดหลายรูปภาพพร้อมกัน สามารถลากวาง (Drag & Drop) จัดเรียงลำดับรูปภาพได้
  - **ฝั่งขวา (ระบบสต็อกและตารางไซส์):**
    - `ประเภทไซส์ (Size Guide):` มีปุ่มสลับระหว่าง **Shoes (รองเท้า)** และ **Apparel (เสื้อผ้า)**
    - `ตารางไซส์ (Size Matrix):` แอดมินสามารถกด `+ Add Column` เพิ่มสเปกความยาว/รอบอก (in/cm) ได้อิสระ และกด `+ เพิ่มตารางไซส์` เพื่อเพิ่มไซส์ใหม่ๆ (เช่น 38, 39, S, M)
    - `สต็อก:` ใส่จำนวนสินค้าที่มีอยู่จริงแยกตามไซส์ที่สร้างไว้ ข้อมูลทั้งหมดนี้จะถูกบันทึกเป็นรูปแบบ JSON ลงฐานข้อมูล (ตาราง `products`, `product_images`, และ `inventory`)

- **โครงสร้างตารางแสดงผลหน้าหลัก (Dynamic Data Grid):**
  - **ข้อมูลพื้นฐาน:** แสดงรูปภาพ Thumbnail, รหัส SKU, ชื่อสินค้า, หมวดหมู่ และแบรนด์
  - **ระบบราคา (Pricing Engine):** แสดงราคาปกติ, ส่วนลด (เช่น -5%), และระบบจะคำนวณ **ราคาสุทธิ (Net Price)** ออกมาแสดงผลอัตโนมัติ (ตัวเลขสีเขียว)
  - **ระบบจัดการไซส์เชิงลึก (Size Variant Badges):** 
    - ไม่ได้แสดงแค่ยอดรวม แต่ระบบจะ **แตกรายการไซส์และจำนวนย่อย** ออกมาเป็น Badge (เช่น `S: 1`, `M: 10`)
    - *Color Logic:* หากไซส์ไหนมีของน้อย ตัวเลขจะเปลี่ยนเป็น **สีแดง** เพื่อเตือนสายตาแอดมินทันที
  - **ยอดรวม (Total Stock):** วงกลมสรุปจำนวนสินค้าทุกไซส์รวมกัน
  - **ชุดคำสั่งจัดการ (Action Buttons):** 
    - `เพิ่มสีสินค้า`: ฟังก์ชันพิเศษสำหรับโคลน (Clone) ข้อมูลสินค้าเดิมเพื่อสร้างตัวเลือกสีใหม่โดยไม่ต้องพิมพ์ข้อมูลใหม่ทั้งหมด
    - `Edit` / `Delete`: สำหรับแก้ไขรายละเอียดเชิงลึกและลบข้อมูลออกจากฐานข้อมูล


- **ระบบออกรายงานและการจัดหน้า (Export & Pagination):**
  - **PDF Generation:** ปุ่ม `พิมพ์รายงาน PDF` แอดมินสามารถดึงข้อมูลตารางปัจจุบันออกเป็นไฟล์เอกสาร เพื่อส่งให้พนักงานคลังสินค้าใช้เดินตรวจนับสต็อกจริง (Physical Stock Count) ได้ทันที
  - **Pagination:** ระบบแบ่งหน้าข้อมูลที่สามารถเลือกระบุหน้าได้ เพื่อไม่ให้เบราว์เซอร์รับภาระหนักเกินไปเมื่อมีสินค้าจำนวนมาก

- **การเชื่อมต่อฐานข้อมูล & เทคโนโลยีเบื้องหลัง (Technical Implementation):**
  - **API Methods (Full Lifecycle):** 
    - **`GET /api/products`**: ดึงข้อมูลรายการสินค้าทั้งหมด พร้อมการทำ JOIN ตารางแบบ Real-time เพื่อแสดงผลในตารางหลัก
    - **`POST /api/products`**: สำหรับการบันทึก (Create) สินค้าใหม่เข้าสู่ระบบ
    - **`PUT /api/products/:id`**: สำหรับการแก้ไข (Update) ข้อมูลสินค้าเดิมที่เลือก
    - **`DELETE /api/products/:id`**: สำหรับการลบข้อมูลออกจากฐานข้อมูล (ถาวร)
  - **Data Format (Multipart/Form-Data):** เนื่องจากมีการอัปโหลดไฟล์รูปภาพ ระบบจึงใช้การส่งข้อมูลแบบ `FormData` ซึ่งรวมทั้งไฟล์ภาพ (File Objects) และข้อมูลข้อความ (Text/JSON) ไว้ใน Request เดียวกัน
  - **Database Transactions:** ข้อมูลจะถูกกระจายไปบันทึกใน 3 ตารางหลัก:
    1. **`products`**: เก็บข้อมูลพื้นฐาน (ชื่อ, ราคา, รายละเอียด, แบรนด์)
    2. **`product_images`**: เก็บ Path หรือ URL ของรูปภาพใน Gallery
    3. **`inventory`**: เก็บความสัมพันธ์ของ ไซส์ และ จำนวนสต็อก ของแต่ละสินค้า
  - **Image Processing:** ฝั่ง Backend ใช้ไลบรารี **Multer** ในการจัดการไฟล์ภาพที่ส่งมาจาก Frontend เพื่อเก็บลงในหน่วยความจำของเซิร์ฟเวอร์ก่อนบันทึก Path ลงฐานข้อมูล
 
### 2.5 ระบบสรุปมูลค่าคลังสินค้าและยอดขาย (Product Inventory & Summary - `/admin/inventory`)
   หน้านี้เป็นหน้าจอแยกเฉพาะสำหรับการทำบัญชีคลังสินค้า (Inventory Accounting) เพื่อวิเคราะห์มูลค่าสินทรัพย์และกระแสเงินสด:
   - **สรุปภาพรวมด้านการเงิน (Financial Header Cards):**
     - **Total Quantity:** จำนวนสินค้าทั้งหมดที่มีในระบบ (ทั้งที่ขายไปแล้วและยังเหลืออยู่)
     - **Sold Revenue (฿):** ยอดเงินรวมที่ขายได้จริงจากออเดอร์ที่สำเร็จ
     - **Stock Value (฿):** มูลค่าของสินค้าที่ยังคงเหลืออยู่ในคลัง (Assets Value)
     - **Grand Total (Stock + Sales):** มูลค่ารวมของธุรกิจ (ยอดขายสะสม + มูลค่าสินค้าคงเหลือ)
   - **ตารางวิเคราะห์รายสินค้า (Inventory & Financial Table):**
     - **ฝั่งจำนวน (Products QTY):** แสดงความสัมพันธ์ระหว่าง `จำนวนทั้งหมด` = `จำนวนที่ขายได้` + `จำนวนที่เหลือ` เพื่อการตรวจสอบยอดสต็อกที่หายไป
     - **ฝั่งการเงิน (Financial Summary):** คำนวณรายได้ (Sold Revenue) และมูลค่าสต็อก (Stock Value) แยกตามรายสินค้า
   - **ระบบออกรายงาน (Data Export):**
     - **Export PDF:** ปุ่มสีม่วงสำหรับดึงข้อมูลสรุปการเงินทั้งหมดออกเป็นไฟล์ PDF เพื่อใช้ในการทำรายงานประจำเดือนหรือส่งฝ่ายบริหาร

### 2.6 ระบบการเงินและรายงานสรุปรายได้ (Financial & Revenue Management - `/admin/revenue`)
หน้าจอส่วนงานบัญชีและบริหาร (Financial Dashboard) สำหรับตรวจสอบกระแสเงินสด ยอดขายสุทธิ และวิเคราะห์แนวโน้มรายได้ของระบบแบบ Real-time:

**ขั้นตอนการทำงาน (Work Procedure):**
1. **Data Fetching:** ระบบดึงข้อมูลจากตาราง `orders` เฉพาะรายการที่ชำระเงินสำเร็จ (`payment_status = 'verified'`)
2. **Aggregation Logic:** ประมวลผลคำนวณยอดรวม (SUM) และนับจำนวนรายการ (COUNT) โดยแบ่งกลุ่มตามช่วงเวลา (รายวัน/เดือน/ปี)
3. **Visualization:** ส่งข้อมูลไปยัง Component กราฟ (`Recharts`) เพื่อวาดเส้นแสดงแนวโน้มรายได้
4. **Comparison:** เปรียบเทียบยอดขายปัจจุบันกับช่วงเวลาก่อนหน้าเพื่อหาอัตราการเติบโต (%)
5. **Reporting:** แอดมินสามารถเลือกช่วงเวลาและกดปุ่ม "พิมพ์รายงาน" เพื่อรับเอกสารสรุปการเงินในรูปแบบ PDF

- **บัตรดัชนีชี้วัดผลงาน (Key Performance Indicators - KPI Cards):**
  - **Today's Revenue:** แสดงยอดขายเฉพาะวันปัจจุบัน พร้อมระบบคำนวณส่วนต่างเปรียบเทียบกับเมื่อวาน (Percentage Change) เพื่อดูความเคลื่อนไหวรายวัน
  - **This Month:** แสดงยอดขายรวมของเดือนปัจจุบัน พร้อมเปรียบเทียบกับเดือนที่ผ่านมา เพื่อวิเคราะห์อัตราการเติบโต (Growth Rate) ของธุรกิจ
  - **Lifetime Revenue & Items Sold:** ยอดรวมสะสมตั้งแต่เริ่มดำเนินกิจการและจำนวนชิ้นสินค้าที่ขายได้จริง เพื่อแสดงถึงสเกลของธุรกิจ

- **ระบบวิเคราะห์สถิติรายได้ (Revenue Statistics Analysis Chart):**
  - **Dynamic Chart Visualization:** ใช้กราฟเส้น (Line Chart) แสดงความสัมพันธ์ระหว่างรายได้และช่วงเวลา ช่วยให้มองเห็นแนวโน้ม (Trend) ได้ชัดเจน
  - **Forecast Engine (Last 7 Days):** ระบบประมวลผลข้อมูลย้อนหลังเพื่อทำพยากรณ์ยอดขายล่วงหน้า ช่วยในการวางแผนสต็อกและการตลาด
  - **Time-Scale Selector:** แอดมินสามารถสลับมุมมองข้อมูลได้ 3 ระดับ: รายสัปดาห์ (Week), รายเดือน (Month) และรายปี (Year)

- **เครื่องมือเสริมด้านการวางแผน (Strategic Planning Tools):**
  - **Daily Sales Calendar:** ปฏิทินแสดงผลการขายรายวัน ซึ่งจะไฮไลท์วันที่มียอดสั่งซื้อ ช่วยให้แอดมินวิเคราะห์ "วันซื้อขายหนาแน่น" (Peak Shopping Days) ได้แม่นยำ
  - **Revenue Strategy Summary:** ส่วนสรุปกลยุทธ์ท้ายหน้าจอที่รวบรวมยอดขายสุทธิและจำนวนหน่วยสินค้าที่จำหน่ายได้ เพื่อใช้เป็นข้อมูลประกอบการตัดสินใจในที่ประชุมบริหาร

- **ระบบออกรายงานและการสั่งพิมพ์ (Professional Reporting & Print Engine):**
  - **Smart Print Layout:** ระบบรองรับการสั่งพิมพ์รายงาน (Print Report) โดยมีการใช้เทคโนโลยี **CSS Print Media Queries** เพื่อปรับแต่งหน้าจอให้เหมาะสมกับกระดาษ A4 โดยอัตโนมัติ
  - **Clean Report Format:** เมื่อสั่งพิมพ์ ระบบจะซ่อนส่วนประกอบที่ไม่จำเป็น (เช่น เมนู Sidebar หรือปุ่มกดต่างๆ) ผ่านคลาส `.no-print-row` ทำให้เหลือเพียงตัวเลขสถิติและกราฟที่สำคัญ เพื่อให้ได้เอกสารรายงานที่ดูเป็นมืออาชีพ
  - **One-Click PDF Export:** แอดมินสามารถบันทึกหน้านี้เป็นไฟล์ PDF ได้ทันทีผ่านฟังก์ชันการพิมพ์ของเบราว์เซอร์ เพื่อใช้เป็นหลักฐานประกอบการทำบัญชีรายเดือน


- **การเชื่อมต่อฐานข้อมูล & เทคนิคเบื้องหลัง (Technical Logic):**
  - **Data Aggregation:** ระบบใช้คำสั่ง SQL `SUM` และ `COUNT` ร่วมกับ `GROUP BY` วัน/เดือน/ปี เพื่อดึงข้อมูลจากตาราง `orders` มาประมวลผลเป็นชุดข้อมูลสถิติ
  - **Backend API:** `GET /api/admin/revenue/stats` (ดึงข้อมูลสรุปการเงิน)
  - **Visual Library:** พัฒนาโดยใช้ **Recharts** ที่รองรับความละเอียดสูงและการตอบสนองต่อขนาดหน้าจอ (Responsiveness)

### 2.7 ระบบวิเคราะห์อันดับสินค้าขายดี (Best Sellers Analytics - `/admin/bestsellers`)
ส่วนงานวิเคราะห์สถิติการจำหน่ายสินค้าเพื่อใช้ในการวางแผนกลยุทธ์การตลาดและบริหารคลังสินค้า โดยระบบจะสรุปข้อมูลยอดขายแยกตามรายสินค้าและแบรนด์แบบ Real-time

- **การแสดงผลทางหน้าจอ (UI Components):**
  - **Top Products Showcase (Podium UI):** แสดงสินค้า 3 อันดับแรกในรูปแบบการ์ดโพเดียม (Champion, 2nd, 3rd) เพื่อเน้นย้ำสินค้าที่เป็นตัวขับเคลื่อนรายได้หลักของร้าน
  - **Performance Status Badges:** ระบบจำแนกสถานะประสิทธิภาพสินค้าอัตโนมัติ ได้แก่:
    - `PEAK`: สินค้าที่มียอดจำหน่ายสูงสุดในระบบ
    - `TOP TIER`: สินค้ากลุ่มยอดนิยมที่มีอัตราการหมุนเวียนสินค้าสูง
  - **Full Catalog Performance Table:** ตารางสรุปรายการสินค้าทั้งหมดในคลัง โดยเรียงลำดับตามปริมาณการขาย (Sales Volume) จากมากไปน้อย

- **ขั้นตอนการทำงานและลอจิก (Logic & Workflow):**
  1. **Data Aggregation:** ระบบคำนวณยอดขายโดยการนับจำนวนชิ้นจากตาราง `order_items` เชื่อมโยงกับ `orders` ที่มีสถานะการชำระเงินเรียบร้อยแล้ว
  2. **Revenue Calculation:** คำนวณรายได้รวมรายชิ้น (Gross Revenue) โดยอ้างอิงจากราคาสินค้า ณ วันที่ทำรายการ (Historical Price)
  3. **Ranking Logic:** จัดลำดับสินค้าตามจำนวนหน่วยที่ขายได้จริง และกำหนดสถานะ Badge (PEAK/TOP TIER) ตามเกณฑ์ที่ตั้งไว้ในระบบ

- **การเชื่อมต่อข้อมูลและเทคโนโลยี (Technical Details):**
  - **API:** `GET /api/admin/bestsellers` (ดึงข้อมูลสรุปยอดขายแยกตามรายการสินค้า)
  - **Database:** ดึงข้อมูลผ่านการ SQL Join ระหว่างตาราง `order_items`, `products` และ `orders`
  - **Reporting:** ฟังก์ชัน **"Print Hub Report"** ใช้ระบบ CSS Media Queries สำหรับการจัดหน้ากระดาษ (Print Layout) เพื่อออกรายงานสรุปสถิติในรูปแบบ PDF


---

## 3. การทำงานฝั่งผู้จัดการสูงสุด (Super Admin Management)
*(โดเมนการใช้งาน: บริหารงานบุคคล, ตรวจจับทุจริต และควบคุมความมั่นคงของระบบ)*

### 3.1 ระบบยืนยันตัวตนด้วยใบหน้าอัจฉริยะ (Biometric AI Face Security - `/superadmin/face-scan`)
ระบบความปลอดภัยขั้นสูงสุดที่ใช้ปัญญาประดิษฐ์ (AI) ในการวิเคราะห์และตรวจสอบอัตลักษณ์บุคคล เพื่ออนุญาตให้เข้าถึงสิทธิ์การจัดการระดับสูง

- **เทคโนโลยีที่ใช้ในการเปิดกล้องและประมวลผล (Core Technology Stack):**
  - **Webcam Interface:** ใช้เทคโนโลยี **WebRTC (navigator.mediaDevices.getUserMedia)** เพื่อเรียกเปิดกล้องวิดีโอจากบราวเซอร์โดยตรง สนับสนุนการทำงานแบบ Real-time Stream
  - **AI Engine:** พัฒนาด้วย **Face-api.js** ซึ่งทำงานบน **TensorFlow.js** ทำให้สามารถรันโมเดล Deep Learning ได้โดยตรงบนเบราว์เซอร์ (Client-side Processing) โดยไม่ต้องส่งข้อมูลภาพออกนอกเครื่องเพื่อความปลอดภัยสูงสุด

- **หลักการทำงานและอัลกอริทึม (Working Principle & Algorithms):**
  1. **Neural Network Model Loading:** ระบบจะทำการโหลดโมเดลอ้างอิง 3 ส่วน:
     - `ssdMobilenetv1`: โมเดลสำหรับการตรวจจับตำแหน่งใบหน้า (Face Detection)
     - `faceLandmark68Net`: โมเดลสำหรับระบุจุดพิกัดสำคัญ 68 จุดบนใบหน้า (Eyes, Nose, Mouth contour)
     - `faceRecognitionNet`: โมเดลสำหรับสร้างอัตลักษณ์ตัวเลข (Face Descriptor) ขนาด 128 มิติ
  2. **Real-time Face Tracking:** เมื่อเปิดกล้อง ระบบจะทำการสร้างกรอบครอบใบหน้า (Bounding Box) และดึงจุดพิกัด Landmark ออกมาเพื่อตรวจสอบทิศทางของใบหน้า
  3. **Stability Logic (3-Second Rule):** ระบบมีการเขียนลอจิกให้นับถอยหลัง **3 วินาที** โดยผู้ใช้ต้องวางใบหน้าให้ตรงตามเงื่อนไขเพื่อป้องกันการใช้ "ภาพถ่าย" (Still Image) มาหลอก และเพื่อให้ค่า Descriptor มีความแม่นยำสูงสุด
  4. **Vector Comparison (Euclidean Distance):** 
     - ระบบจะดึงค่าพิกัดต้นฉบับจากฐานข้อมูลมาเปรียบเทียบกับค่าที่สแกนได้ ณ ปัจจุบัน
     - ใช้อัลกอริทึมคำนวณระยะห่างระหว่างจุดในมิติสูง (Euclidean Distance) โดยมีค่า **Threshold อยู่ที่ 0.55** (หากค่าน้อยกว่านี้ถือเป็นคนเดียวกัน)

- **รายละเอียดทางเทคนิค (Technical Details):**
  - **API:** `GET /api/auth/face-data` (ดึงพิกัดใบหน้าต้นฉบับ), `PUT /api/auth/face-data` (ลงทะเบียนใบหน้าใหม่)
  - **Database:** เก็บข้อมูล Vector เป็นรูปแบบ JSON Array (Float32Array) ในตาราง `users` ฟิลด์ `face_descriptor`
  - **Security Guard:** เมื่อการสแกนสำเร็จ ระบบจะทำการ Redirect ไปยังหน้าจัดการที่ต้องการพร้อมแนบสถานะ Verified ใน Application State

### 3.2 ระบบบริหารจัดการบุคลากร (Workforce Management - `/superadmin/manage`)
ส่วนควบคุมพนักงานและผู้ดูแลระบบทั้งหมด ซึ่งเป็นด่านแรกในการกำหนดความปลอดภัยขององค์กรและการเข้าถึงข้อมูลสำคัญ

- **บัตรสรุปสถานะกำลังพล (Overview Cards):**
  - **จำนวนบุคลากรทั้งหมด:** แสดงยอดรวมของพนักงานและผู้ดูแลระบบทุกคน
  - **ซูเปอร์แอดมิน:** แสดงจำนวนผู้ถือสิทธิ์สูงสุดที่สามารถจัดการระบบหลังบ้านได้ทั้งหมด
  - **ลงทะเบียนใบหน้าแล้ว:** จำนวนแอดมินที่ผ่านการยืนยันอัตลักษณ์ผ่านระบบ AI เรียบร้อยแล้ว

- **รายชื่อบุคลากรผู้มีสิทธิ์เข้าถึง (Personnel Access List):**
  - **สิทธิ์การเข้าถึง (Permissions):** แบ่งระดับด้วย Badge สีที่ชัดเจน ได้แก่ `SUPERADMIN` และ `ADMIN`
  - **ข้อมูลใบหน้า (Biometrics Status):** 
    - `ลงทะเบียนแล้ว` (สีเขียว): มีฐานข้อมูลใบหน้าพร้อมสำหรับการตรวจสอบความปลอดภัย
    - `มาตรฐาน` (สีเทา): ยังไม่ได้ลงทะเบียนใบหน้า (สิทธิ์การเข้าถึงจะถูกจำกัดตามนโยบายความปลอดภัย)

- **ชุดคำสั่งจัดการข้อมูล (Management Actions):**
  - **+ เพิ่มแอดมินใหม่:** ปุ่มสำหรับสร้างบัญชีผู้ใช้งานใหม่เข้าสู่ระบบ (`POST /api/admin/users`)
  - **Reset Biometric (ไอคอนรีเฟรช):** สำหรับล้างข้อมูลใบหน้าเดิมทิ้งเพื่อให้แอดมินลงทะเบียนใหม่
  - **Face Scan (ไอคอนกล้อง):** ทางลัดเข้าสู่ระบบสแกนใบหน้าเพื่อบันทึกอัตลักษณ์บุคคล
  - **แก้ไข & ลบ:** การจัดการข้อมูลพื้นฐานของบุคลากรรายบุคคล (CRUD Operations)

- **รายละเอียดทางเทคนิค (Technical Details):**
  - **API:** `GET /api/admin/all` (ดึงรายชื่อแอดมินทั้งหมด), `PUT /api/admin/update-role` (เปลี่ยนระดับสิทธิ์)
  - **Database:** ดึงข้อมูลจากตาราง **`users`** โดยกรองเฉพาะคนที่มี `role` ไม่ใช่ 'user'
  - **Security Logic (No Face, No Super Admin):** ระบบ Frontend และ Backend จะตรวจสอบร่วมกัน หากฟิลด์ `face_descriptor` ในฐานข้อมูลเป็นค่าว่าง (null) ระบบจะไม่อนุญาตให้เปลี่ยนสถานะเป็น Superadmin ได้อย่างเด็ดขาด เพื่อป้องกันช่องโหว่ทางความปลอดภัย

### 3.3 ระบบบันทึกกิจกรรมและการตรวจสอบความเปลี่ยนแปลง (Advanced Activity Logs - `/admin/logs`)
ศูนย์กลางการตรวจสอบ (Audit Trail) ที่อนุญาตให้เข้าถึงได้เฉพาะผู้จัดการสูงสุด (**Super Admin**) เพื่อติดตามความเคลื่อนไหวทุกอย่างภายในระบบ ป้องกันการทุจริตและตรวจสอบความผิดพลาดย้อนหลัง

- **สิทธิ์การเข้าถึงและการควบคุม (Security & Permission):**
  - **Super Admin Only:** เมนูนี้จะถูกซ่อนจาก Sidebar ของแอดมินทั่วไป และมีการทำ **Middleware Guard** ใน Backend เพื่อดักจับ Token หากไม่ใช่ Role 'superadmin' จะไม่สามารถดึงข้อมูลผ่าน API ได้

- **ลอจิกการทำงานและการดึงข้อมูล (Logic & Data Retrieval):**
  1. **Event Interceptor:** ทุกครั้งที่มีการเรียกใช้ API ประเภท `POST`, `PUT`, หรือ `DELETE` ในระบบจัดการสินค้าหรือคำสั่งซื้อ Backend จะทำการบันทึก Log อัตโนมัติลงในตาราง `activity_logs`
  2. **Data Snapshotting:** ระบบจะเก็บข้อมูลในรูปแบบ **JSON Object** ของสิ่งที่เกิดขึ้น เช่น ข้อมูลสินค้าเดิมก่อนแก้ไข และข้อมูลใหม่หลังแก้ไข
  3. **Changes Tracking Logic:** ในกรณีการ `Update` ระบบจะใช้ฟังก์ชันเปรียบเทียบ (Comparison) เพื่อระบุว่าฟิลด์ไหนมีการเปลี่ยนแปลงบ้าง (เช่น สต็อกลดลงจาก 5 เหลือ 2) แล้วจึงบันทึกผลต่างนั้นลงในคอลัมน์ `Details`

- **การแสดงผลทางหน้าจอ (UI Presentation):**
  - **Nested Data Display:** ระบบจะทำการ **Parse JSON** จากฐานข้อมูลมาแสดงผลเป็นตารางย่อย (Nested Table) ภายใต้แถวของ Log นั้นๆ ทำให้เห็นรายละเอียดชัดเจนโดยไม่ต้องเปลี่ยนหน้า
  - **Action Badges:** ใช้สีจำแนกประเภทกิจกรรม เช่น `Create` (เขียว), `Update` (น้ำเงิน), `Delete` (แดง) เพื่อความรวดเร็วในการตรวจสอบ

- **รายละเอียดทางเทคนิค (Technical Details):**
  - **API:** `GET /api/admin/logs` (ดึงข้อมูลประวัติกิจกรรมทั้งหมด)
  - **Database:** ตาราง `activity_logs` เก็บข้อมูล `id`, `admin_id`, `action`, `target_entity`, `target_id`, `details` (Type: JSON), และ `timestamp`
  - **Data Integrity:** ข้อมูลในหน้านี้เป็นแบบ **Read-only** ไม่สามารถแก้ไขหรือลบได้จากหน้า UI เพื่อให้คงสถานะความเป็นหลักฐาน (Anti-tampering)

---

## 4. โครงสร้างความต้านทานและความปลอดภัย (Technical Resilience & Security Stack)

เพื่อความเสถียรและมาตรฐานความปลอดภัยระดับสูง ระบบได้รับการออกแบบโดยครอบคลุมกลไกดังนี้:

1. **Frontend Isolation Route (React Route Guard):**
   การใช้เทคโนโลยี `ProtectedRoute` ในการห่อหุ้มเส้นทางของ Admin และ Super Admin ทั้งหมด หากระบบตรวจสอบไม่พบ JSON Web Token (JWT) ที่ถูกต้อง ระบบจะทำการดีด (Redirect) ผู้ใช้กลับสู่หน้าหลักทันที เพื่อป้องกันการเข้าถึงหน้า UI โดยไม่ได้รับอนุญาตผ่านการพิมพ์ URL โดยตรง

2. **Database Integrity Layer (SQL Transactions):** 
   ในกระบวนการที่มีความสำคัญสูง เช่น การสั่งซื้อ (Checkout) ซึ่งต้องมีการตัดสต็อกและบันทึกออเดอร์พร้อมกัน ระบบจะใช้กลไก `beginTransaction` หากขั้นตอนใดขั้นตอนหนึ่งเกิดข้อผิดพลาด ระบบจะทำการ `Rollback` ข้อมูลทั้งหมดกลับสู่สถานะเริ่มต้น เพื่อป้องกันการเกิดข้อมูลค้างในระบบ (Data Inconsistency)

3. **Cryptographic Credential Protection (Bcrypt Hashing):**
   รหัสผ่านของผู้ใช้งานทุกคนจะผ่านกระบวนการ Salt และ Hash ด้วยอัลกอริทึม **Bcrypt** ก่อนการบันทึกลงฐานข้อมูล เพื่อรับประกันว่าต่อให้ข้อมูลรั่วไหล รหัสผ่านจริงของผู้ใช้จะยังคงเป็นความลับและไม่สามารถถอดรหัสออกมาได้ (Plaintext Protection)

4. **Environment Variable Encapsulation (.env Management):**
   ข้อมูลสำคัญระดับระบบ เช่น รหัสผ่านฐานข้อมูล, JWT Secret Key และคีย์การเชื่อมต่ออื่นๆ จะถูกแยกเก็บไว้ในรูปแบบ Environment Variables ไม่มีการบันทึกลงในซอร์สโค้ดโดยตรง เพื่อป้องกันความเสี่ยงในการถูกขโมยข้อมูลผ่านทางเวอร์ชันคอนโทรล (Git)

5. **Token-Based Authentication & API Authorization:**
   การสื่อสารระหว่าง Frontend และ Backend ทั้งหมดจะมีการแนบ JWT ไปใน Header ของคำขอ (Request) โดยฝั่ง Backend จะมี **Middleware** ทำการตรวจสอบความถูกต้องของ Token และระดับสิทธิ์ (Role) ก่อนที่จะอนุญาตให้เข้าถึงข้อมูลในฐานข้อมูลทุกครั้ง

6. **Cross-Origin Resource Sharing (CORS) Policy:**
   Backend ได้กำหนดนโยบาย CORS เพื่อจำกัดสิทธิ์ให้เฉพาะโดเมนของ Frontend ที่ได้รับอนุญาตเท่านั้นที่สามารถส่งคำร้องขอข้อมูลได้ เพื่อป้องกันการโจมตีจากเว็บไซต์ภายนอก (Cross-site attacks)
