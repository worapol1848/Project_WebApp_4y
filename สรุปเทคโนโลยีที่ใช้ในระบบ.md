# สรุปเทคโนโลยีและไลบรารีหลักที่ใช้ในระบบ Velin Inventory & E-Commerce

เอกสารนี้จัดทำขึ้นเพื่ออธิบายถึงเหตุผลในการเลือกใช้เทคโนโลยีต่างๆ ภายในระบบ ไม่ว่าจะเป็นระบบรักษาความปลอดภัย (JWT, สแกนใบหน้า) ระบบออกรายงาน (PDF) และระบบจัดการพิกัดที่อยู่ (Map) โดยมีรายละเอียดเชิงลึกดังต่อไปนี้:

---

## 1. ระบบยืนยันตัวตนด้วย JWT (JSON Web Token)
**ไลบรารีที่ใช้:** `jsonwebtoken` (ฝั่ง Backend)

### ทำไมถึงใช้ JWT?
*   **Stateless (ไม่เก็บสถานะบนเซิร์ฟเวอร์):** JWT จะเก็บข้อมูลสิทธิ์ของผู้ใช้ไว้ในตัว Token เอง ทำให้เซิร์ฟเวอร์ไม่ต้องจดจำ Session ของผู้ใช้แต่ละคน ช่วยลดภาระของหน่วยความจำเซิร์ฟเวอร์ (Scalability) และทำให้ระบบทำงานได้รวดเร็วขึ้น
*   **ความปลอดภัย (Security):** ข้อมูลใน JWT จะถูกเซ็นกำกับ (Sign) ด้วย Secret Key ทำให้หากมีใครพยายามแก้ไขข้อมูลใน Token (เช่น พยายามเปลี่ยนสิทธิ์ตัวเองเป็น Admin) เซิร์ฟเวอร์จะรู้ทันทีและปฏิเสธการเข้าถึง
*   **Cross-Domain & โมบายล์แอป:** JWT สามารถส่งผ่าน HTTP Header (`Authorization: Bearer <token>`) ได้ง่าย ทำให้รองรับการเชื่อมต่อกับ Frontend ที่อยู่คนละโดเมน (CORS) หรือนำไปต่อยอดทำ Mobile App ได้สะดวก

### นำมาใช้ทำอะไรในระบบนี้?
*   **การเข้าสู่ระบบ (Login):** เมื่อผู้ใช้ (User) หรือผู้ดูแลระบบ (Admin) ล็อกอินสำเร็จ ระบบ Backend จะสร้าง JWT ที่บรรจุข้อมูลพื้นฐาน เช่น `id`, `role` ส่งกลับไปให้ Frontend
*   **ปกป้อง API (Route Protection):** ในไฟล์ `authMiddleware.js` จะคอยดักจับและตรวจสอบ Token ทุกครั้งที่มีการเรียกใช้งาน API ที่สำคัญ หากไม่มี Token หรือ Token หมดอายุ/ไม่ถูกต้อง จะถูกปฏิเสธการเข้าถึง
*   **จัดการสิทธิ์ (Role-based Access Control):** ใช้แยกระดับการเข้าถึงระหว่าง User ธรรมดา, Admin และ Super Admin ผ่านข้อมูล `role` ที่ฝังอยู่ใน Token

---

## 2. ระบบสร้างเอกสารและรายงานด้วย PDF
**ไลบรารีที่ใช้:** `jspdf` และ `jspdf-autotable` (ฝั่ง Frontend)

### ทำไมถึงใช้ไลบรารีนี้?
*   **ทำงานบนเบราว์เซอร์ (Client-side Generation):** `jspdf` สามารถสร้างไฟล์ PDF ได้โดยตรงจากเบราว์เซอร์ของผู้ใช้โดยไม่ต้องส่งข้อมูลกลับไปให้เซิร์ฟเวอร์ประมวลผล ช่วยลดภาระของ Backend และสร้างเอกสารได้ทันที
*   **การจัดรูปแบบตารางที่สวยงาม:** ใช้ร่วมกับ `jspdf-autotable` เพื่อแปลงข้อมูลตาราง (Table) หรือ JSON ให้กลายเป็นตารางในไฟล์ PDF ได้อย่างเป็นระเบียบ รองรับการปรับแต่งสี ฟอนต์ และขนาด

### นำมาใช้ทำอะไรในระบบนี้?
ระบบนำมาใช้ในส่วนของ **Admin Dashboard** สำหรับออกรายงานสรุปผลต่างๆ เพื่อให้ผู้ดูแลระบบสามารถดาวน์โหลด พิมพ์ หรือนำไปรายงานผู้บริหารต่อได้ ได้แก่:
*   `AdminRevenue.jsx`: รายงานสรุปยอดขายและรายได้
*   `AdminProducts.jsx`: รายการสินค้าทั้งหมดในระบบ
*   `AdminInventory.jsx`: รายงานสถานะคลังสินค้าและสต็อกคงเหลือ
*   `AdminBestSellers.jsx`: รายงานจัดอันดับสินค้าขายดี

---

## 3. ระบบแผนที่แสดงพิกัดจัดส่ง (Map)
**ไลบรารีที่ใช้:** `leaflet` และ `react-leaflet` (ฝั่ง Frontend)

### ทำไมถึงใช้ไลบรารีนี้?
*   **Open Source และใช้งานฟรี:** Leaflet เป็นไลบรารีแผนที่แบบ Open Source ที่ไม่ต้องเสียค่า API Key (ต่างจาก Google Maps API) ทำให้ประหยัดต้นทุนของโปรเจกต์
*   **น้ำหนักเบาและประสิทธิภาพสูง:** โหลดทำงานได้รวดเร็ว และมี `react-leaflet` ที่ทำหน้าที่แปลงฟังก์ชันของ Leaflet ให้อยู่ในรูปแบบ React Components ทำให้เขียนโค้ดและจัดการ State ได้ง่ายขึ้น
*   **รองรับแผนที่หลายแบบ (Tile Providers):** สามารถดึงข้อมูลภาพแผนที่จาก OpenStreetMap (OSM) มาแสดงผลได้อย่างแม่นยำ

### นำมาใช้ทำอะไรในระบบนี้?
ระบบนำแผนที่มาใช้เพื่อเพิ่มความแม่นยำในการระบุตำแหน่งจัดส่งสินค้า และเพิ่มประสบการณ์ที่ดีให้ผู้ใช้ (UX) ดังนี้:
*   `Profile.jsx`: ให้ลูกค้าสามารถปักหมุดตำแหน่งที่อยู่ของตนเองบนแผนที่ได้ เพื่อความแม่นยำในการจัดส่ง
*   `Payment.jsx`: แสดงแผนที่ยืนยันที่อยู่จัดส่งในขั้นตอนก่อนชำระเงิน
*   `OrderSuccess.jsx`: แสดงแผนที่และตำแหน่งที่คำสั่งซื้อนั้นกำลังจะไปส่ง เพื่อความอุ่นใจของลูกค้า
*   `AdminOrders.jsx`: ให้ผู้ดูแลระบบสามารถดูพิกัดปลายทางของลูกค้าแต่ละออเดอร์บนแผนที่ เพื่อวางแผนการจัดส่ง

---

## 4. ระบบสแกนใบหน้า (Biometrics / Face Scan)
**ไลบรารีที่ใช้:** `@vladmandic/face-api` (ฝั่ง Frontend)

### ทำไมถึงใช้ไลบรารีนี้?
*   **ประมวลผลภาพบนเบราว์เซอร์:** เป็นไลบรารีที่รันโมเดล Machine Learning (TensorFlow.js) บนเบราว์เซอร์ (Client-side) ทำให้สามารถตรวจจับและจดจำใบหน้าได้แบบ Real-time จากกล้องเว็บแคมโดยตรง
*   **ความปลอดภัยและความเป็นส่วนตัว:** ข้อมูลภาพวิดีโอไม่ต้องถูกส่งสตรีมไปหาเซิร์ฟเวอร์ตลอดเวลา การคำนวณลักษณะใบหน้า (Face Descriptors) เกิดขึ้นบนเครื่องของผู้ใช้ ทำให้ปลอดภัยและเร็วกว่า

### นำมาใช้ทำอะไรในระบบนี้?
*   **ความปลอดภัยระดับสูงสุดสำหรับ Super Admin:** ใช้ในไฟล์ `FaceScanner.jsx` เพื่อทำเป็น **การยืนยันตัวตนแบบชีวมาตร (Biometric Authentication)**
*   แทนที่จะใช้แค่ Username และ Password ซึ่งอาจถูกขโมยได้ ระบบบังคับให้ผู้ที่มีสิทธิ์ระดับ `superadmin` (ที่มีอำนาจสูงสุดในการจัดการระบบ) ต้องทำการสแกนใบหน้าเพื่อเข้าสู่ระบบ เพื่อป้องกันไม่ให้บุคคลอื่นแอบอ้างสิทธิ์เข้ามาแก้ไขข้อมูลสำคัญของระบบ (ตามข้อกำหนดของ Admin Biometric Management)

---

## 5. ภาพรวมเทคโนโลยีทั้งหมดในระบบ (Full Technology Stack)

**ส่วนหน้าบ้าน (Frontend / Client-Side)**
*   **React (v18):** ไลบรารีหลักสำหรับการสร้าง User Interface (UI) แบบ Component-based
*   **Vite:** เครื่องมือ Build Tool และ Bundler ที่ช่วยให้การพัฒนา React รวดเร็วมาก (เร็วกว่า Create-React-App)
*   **React Router DOM:** จัดการเรื่องระบบ Routing ช่วยให้เปลี่ยนหน้าเว็บได้รวดเร็วโดยไม่ต้องรีเฟรชหน้าเบราว์เซอร์
*   **Axios:** เครื่องมือสำหรับส่ง HTTP Requests ไปเชื่อมต่อและพูดคุยกับ API หลังบ้าน
*   **Recharts:** ไลบรารีสำหรับสร้างกราฟแสดงผลข้อมูลสถิติ
*   **@hello-pangea/dnd:** ระบบลากวาง (Drag and Drop) เพิ่มลูกเล่นในการจัดการข้อมูล
*   **CSS (Vanilla CSS):** ภาษาที่ใช้จัดสไตล์ ตกแต่งหน้าตาของเว็บให้สวยงามและเป็นเอกลักษณ์

**ส่วนหลังบ้าน (Backend / Server-Side)**
*   **Node.js:** Runtime Environment สำหรับรัน JavaScript บนฝั่งเซิร์ฟเวอร์
*   **Express.js (v5):** Web Framework สำหรับสร้าง RESTful API และจัดการ Routing ฝั่งหลังบ้าน
*   **Sequelize (ORM):** เครื่องมือ Object-Relational Mapping ที่ใช้จัดการฐานข้อมูลเชิงสัมพันธ์ผ่าน JavaScript
*   **MySQL2:** ไดรเวอร์ที่เชื่อมให้ Node.js พูดคุยกับฐานข้อมูล MySQL ได้
*   **Bcryptjs:** ระบบเข้ารหัสผ่าน (Password Hashing) เพื่อความปลอดภัย
*   **Multer:** จัดการการอัปโหลดไฟล์ต่างๆ เข้าสู่เซิร์ฟเวอร์ (เช่น อัปโหลดรูปภาพสินค้า, สลิปโอนเงิน)
*   **Cors:** ระบบรักษาความปลอดภัยและอนุญาตให้ Frontend สามารถพูดคุยกับ Backend ที่ทำงานอยู่คนละพอร์ตกันได้
*   **Dotenv:** ใช้เก็บตัวแปรสภาพแวดล้อม (.env)
*   **UUID:** ตัวสร้างรหัสไอดีแบบสุ่มที่ไม่ซ้ำกันในโลก

**ฐานข้อมูล (Database & Tools)**
*   **MySQL:** ระบบจัดการฐานข้อมูลเชิงสัมพันธ์ (RDBMS) หลักของระบบ
*   **XAMPP:** โปรแกรมชุดเครื่องมือจำลองเซิร์ฟเวอร์บนเครื่องคอมพิวเตอร์ส่วนตัว (Localhost)

---

## 6. เหตุผลที่เลือกใช้ JS, JSX, CSS และ XAMPP

### ทำไมถึงใช้ JS (JavaScript)?
*   **Full-Stack JavaScript:** การใช้ JS ทำให้โปรแกรมเมอร์สามารถเขียนได้ทั้ง Frontend (React) และ Backend (Node.js) ด้วยภาษาเดียวกัน ช่วยให้การพัฒนาไหลลื่น
*   **ทำงานแบบ Asynchronous ได้ดี:** Node.js เด่นเรื่องการทำงานแบบ Non-blocking I/O ซึ่งเหมาะกับการทำเว็บ E-Commerce ที่ต้องรับส่งข้อมูลจำนวนมากได้รวดเร็ว

### ทำไมถึงใช้ JSX (JavaScript XML)?
*   **รวมโครงสร้างและตรรกะไว้ด้วยกัน:** JSX อนุญาตให้เอาโค้ดโครงสร้าง HTML มาเขียนปนใน JS ได้เลย ทำให้การสร้าง Component และจัดการข้อมูล (State) ทำได้ง่ายและเห็นภาพรวมชัดเจน
*   **ปลอดภัยจากแฮกเกอร์ (XSS):** React ควบคุมการเรนเดอร์ JSX ทำให้สามารถป้องกันการโจมตีแบบแอบฝังสคริปต์อันตรายได้อัตโนมัติ

### ทำไมถึงใช้ CSS (Vanilla CSS)?
*   **อิสระในการออกแบบระดับสูงสุด:** การเขียน CSS ดิบโดยไม่พึ่งพาเฟรมเวิร์ก (เช่น Bootstrap/Tailwind) ทำให้ควบคุมหน้าตาเว็บ (UI) ได้ 100% สามารถทำสไตล์พรีเมียมเฉพาะตัว เช่น ลูกเล่นแสงเงา หรือการไล่สีได้อย่างอิสระ
*   **ประสิทธิภาพดี เว็บเบา:** ระบบไม่ต้องโหลดไฟล์ CSS สำเร็จรูปขนาดใหญ่มาทั้งหมด ทำให้หน้าเว็บโหลดเร็วขึ้น

### ทำไมถึงใช้ XAMPP?
*   **ง่ายต่อการตั้งค่าฐานข้อมูล (Localhost):** XAMPP มัดรวมบริการ MySQL Server มาให้ในตัว ทำให้ตอนพัฒนาสามารถเปิดใช้งานฐานข้อมูลได้ในคลิกเดียว
*   **มี phpMyAdmin ในตัว:** เครื่องมือนี้ช่วยให้สามารถจัดการตารางข้อมูล เพิ่ม ลบ แก้ไข ข้อมูลผู้ใช้หรือสินค้า ผ่านหน้าเว็บเบราว์เซอร์ได้สะดวกสบาย โดยไม่ต้องพิมพ์คำสั่ง SQL แบบ Manual

---

## 7. โครงสร้างและการจัดการ API (RESTful API Architecture)

ระบบนี้ใช้สถาปัตยกรรมแบบ **RESTful API** ในการเชื่อมต่อสื่อสารระหว่างหน้าบ้าน (Frontend) และหลังบ้าน (Backend) โดยมีรายละเอียดดังนี้:

### เทคโนโลยีที่ใช้จัดการ API:
*   **Express.js:** ทำหน้าที่เป็นตัวจัดการเส้นทาง (Routing) และประมวลผลคำขอ (Request) จากหน้าบ้าน
*   **Axios:** ใช้ฝั่ง Frontend เพื่อส่งคำขอแบบ HTTP (GET, POST, PUT, DELETE) ไปยัง Backend
*   **Sequelize:** ทำหน้าที่ดึงข้อมูลจาก Database มาตอบกลับในรูปแบบ JSON

### โมดูล API หลักในระบบ:
1.  **Authentication API (`auth.js`):** จัดการระบบสมัครสมาชิก, เข้าสู่ระบบ, และการยืนยันตัวตนด้วยใบหน้า (Face Verification)
2.  **Product Management API (`products.js`):** จัดการข้อมูลสินค้า, หมวดหมู่, และสต็อกสินค้า
3.  **Order & Payment API (`orders.js`):** จัดการคำสั่งซื้อ, การแนบสลิปชำระเงิน, สถานะการจัดส่ง และระบบการคืนเงิน (Refund)
4.  **Dashboard & Analytics API (`dashboard.js`):** รวบรวมข้อมูลสรุปยอดขาย, สถิติสินค้าขายดี และข้อมูลภาพรวมสำหรับผู้ดูแลระบบ
5.  **User Interactions API (`cart.js`, `wishlist.js`, `comments.js`):** จัดการตะกร้าสินค้า, รายการสินค้าที่สนใจ และการแสดงความคิดเห็น/รีวิวสินค้า
6.  **System Logs API (`logs.js`):** บันทึกประวัติการทำงานที่สำคัญภายในระบบเพื่อความโปร่งใสและการตรวจสอบ
